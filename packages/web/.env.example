# Base URL of the marketplace REST API (server-side fetches).
MARKETPLACE_API_URL=http://localhost:3100

# Public-facing base URL of the web app itself; used to build deep links the agent
# can hand to the human ("see the search and the products I found here").
NEXT_PUBLIC_WEB_BASE_URL=http://localhost:3200
