# deploy/ contains both committed runbooks and uncommitted secrets.
# Belt-and-braces: this gitignore lives next to the secrets.
.env
.env.local
.env.*.local
*.pem
*.key
*_rsa
*_ed25519
!*_ed25519.pub
